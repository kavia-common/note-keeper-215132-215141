# Backend environment variables for Notes API
# If not provided, the app will default to a local SQLite database at ./notes.db
DATABASE_URL=sqlite:///./notes.db
# Example for Postgres:
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/notesdb
